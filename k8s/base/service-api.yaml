apiVersion: v1
kind: Service
metadata:
  name: coderag-api
  namespace: coderag
spec:
  selector:
    app: coderag-api
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
  type: ClusterIP
