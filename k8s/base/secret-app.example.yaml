apiVersion: v1
kind: Secret
metadata:
  name: coderag-app-secrets
  namespace: coderag
type: Opaque
stringData:
  OPENAI_API_KEY: ""
  GEMINI_API_KEY: ""
  VERTEX_SERVICE_ACCOUNT_JSON_B64: ""
  VERTEX_PROJECT_ID: ""
  NEO4J_PASSWORD: "REPLACE_ME"
  # Auth for the remote Chroma server (mitigation for CVE-2026-45829: the
  # server must reject unauthenticated requests). Use either a bearer token
  # or basic-auth credentials; leave the unused mechanism empty.
  CHROMA_TOKEN: ""
  CHROMA_USERNAME: ""
  CHROMA_PASSWORD: "REPLACE_ME"
